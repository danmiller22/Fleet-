{
  "name": "fleet-ui",
  "version": "0.1.0",
  "private": true,
  "description": "TranGo TMS demo SPA + Node API server",
  "scripts": {
    "build": "esbuild app.js --bundle --format=esm --jsx=automatic --loader:.js=jsx --outfile=dist/app.js",
    "prestart": "npm run build",
    "start": "node server/api-server.js",
    "dev": "node server/api-server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
